module.exports = {
    DB_URL : "mongodb://localhost/crmdb"//so the mongodb database that we created is crmdb and to connect to that we need to pass this url
}                                      //so even if this crmdb is not created earlier it will be created by mongodb